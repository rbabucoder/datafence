{
  "name": "datafence",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass ./sass/style.scss:./css/style.css --style compressed",
    "build-css": "npm run sass",
    "wsass": "sass ./sass/style.scss:./css/style.css --style compressed --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.77.1"
  }
}
